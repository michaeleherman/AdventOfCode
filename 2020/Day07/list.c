#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct node {
    struct nodes **children;
    char bag[64];
    int bagCount;
} node;






int main() {

    node *test = NULL;

    test = (node *)malloc(sizeof(node));

    char *test.bag = "Test bag";
    test.bagCount = 2;
    


    return 0;
}